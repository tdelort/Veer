#ifndef CORE_CONTAINER_HEAP_ARRAY_HPP_INCLUDED
#define CORE_CONTAINER_HEAP_ARRAY_HPP_INCLUDED

#include "heap_array.h"

namespace veer
{
	template<typename T>
	heap_array<T>::heap_array(const T& _val, size_t _size )
	{

	}

	template<typename T>
	heap_array<T>::heap_array(std::initializer_list<T> _list)
	{

	}

	template<typename T>
	heap_array<T>::heap_array(const heap_array<T>& _other)
	{

	}

	template<typename T>
	heap_array<T>& heap_array<T>::operator=(const heap_array<T>& _other)
	{

	}

	template<typename T>
	heap_array<T>::heap_array(heap_array<T>&& _other)
	{

	}

	template<typename T>
	heap_array<T>& heap_array<T>::operator=(heap_array<T>&& _other)
	{

	}

}
#endif // CORE_CONTAINER_HEAP_ARRAY_HPP_INCLUDED
